def execute(command)
    system "#{command}"
  end
  
  desc 'Jekyll Serve'
  task :serve do
    execute(" bundle exec jekyll serve --config _config.yml,_config.dev.yml --incremental --livereload --port 8000")
  end